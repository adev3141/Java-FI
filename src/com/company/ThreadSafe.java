package com.company;

public class ThreadSafe {
    //a thread safe object is that which is a blueprint of a synchronized class
    // two threads cant change its state or properties
    //only one thread can
    //by default JAVA COLLECTIONS CLASS is non synchronized meaning they are not thread safe
}
